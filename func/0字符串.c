/*Q:输入正整数2<n<100;独占一行；
 ×打印出n行或带有空字符的字符串；
 *每行字符串独占一行；
 *C11已禁止使用gets;
*/


#include<stdio.h>

char Char[Max];
int k=-1;
for i=0 to n-1;
  for j=0 to Max{
    getchar("%c",&Char2[i]);
  }
  k++;
  Len[k]=strlen(Char);
  for m=0 to Len[k]-1;
     Str[k].Char[m]=Char2[m];
   }
}

if(=' ')
